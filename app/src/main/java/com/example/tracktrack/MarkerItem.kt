package com.example.tracktrack

/**
 * Represents a marker item that be can bought from the in-game shop.
 */
class MarkerItem(var name: String, var markerResource: Int, var price: Int, var type: String)
